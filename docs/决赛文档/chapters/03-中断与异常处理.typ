= 3. 中断与异常处理
<3-中断与异常处理>
#quote(block: true)[
本章回答：用户态或内核态发生 trap 后，RespOS 如何保存现场、识别原因、调用相应内核服务，并在保证现场完整的前提下返回？
]

== 3.1 模块定位
<31-模块定位>
中断与异常处理是用户程序进入内核的入口，也是时钟、系统调用、缺页和进程抢占等机制的连接点。RespOS 将中断与异常处理过程分为两层：架构相关的入口与返回代码负责完成寄存器保存、特权级切换和硬件状态恢复；架构无关的 Rust 代码负责识别异常原因，并调用系统调用、内存管理、信号和调度模块。

这种划分使得两种架构可以共享同一套处理策略，同时保留各自必要的硬件细节。图 3-1 展示了一次用户态 trap 的完整路径。

```text
用户程序
   │  ecall / 缺页 / 定时器中断
   ▼
架构入口汇编：切换栈、保存寄存器与状态
   ▼
Rust trap_handler：读取原因并分发
   ├─ 系统调用 → syscall → 写回返回值
   ├─ 缺页异常 → MemorySet::handle_page_fault
   ├─ 定时器中断 → 重设时钟、检查计时器、触发抢占
   ├─ 非法指令/断点 → 退出或处理调试事件
   └─ 软件中断 → 响应核间通知
   ▼
信号处理与调度检查
   ▼
架构返回汇编：恢复现场、恢复用户态入口、sret/ertn
```

图 3-1 用户态 trap 的入口、分发与返回路径

== 3.2 Trap 的基本流程
<32-trap-的基本流程>
一次 trap 可以概括为"入口保存、Rust 分发、后置检查、返回恢复"四个阶段。系统调用、缺页、非法指令和断点属于同步异常，定时器和软件中断则可能在程序执行过程中异步到达；无论来源如何，最终都进入同一条处理链路。

首先，硬件将返回地址和异常原因写入架构寄存器，并跳转到预先配置的 trap 入口。入口汇编切换到内核栈，保存通用寄存器和返回所需的状态，构造 `TrapContext`，再调用 Rust 侧的 `trap_handler`。RISC-V 和 LoongArch 使用的寄存器与入口指令不同，但这一阶段都只负责建立一份完整、可恢复的内核现场。

随后，`trap_handler` 读取硬件原因并完成分发：系统调用交给 syscall 层，缺页交给 `MemorySet`，定时器负责设置下一次触发并推进超时处理，软件中断负责响应核间通知。处理结果可能是修改 `TrapContext` 后继续执行，也可能是向用户任务递送信号、结束任务，或在安全点进入调度器。用户态异常通常通过信号或退出隔离应用故障；内核态发生非法指令、缺页或系统调用则按内核错误处理。

在返回前，内核统一完成待处理信号和必要的调度检查。若当前任务仍可运行，返回汇编就按照固定布局恢复 `TrapContext`，切回用户态 trap 入口，并执行 RISC-V 的 `sret` 或 LoongArch 的 `ertn`；若任务已经阻塞、退出或被抢占，则先由调度器切换到其他任务，再从新任务的现场恢复。系统调用返回值、缺页修复结果以及信号处理后的用户态控制流，最终都通过这份可修改的现场生效。

== 3.3 TrapContext：统一的可恢复现场
<33-trapcontext统一的可恢复现场>
`TrapContext` 描述了用户程序的运行状态，定义了入口汇编与 Rust 处理逻辑之间的数据契约。入口汇编按照固定顺序把寄存器保存到内核栈，Rust 代码通过这个结构读取系统调用参数、修改返回值或调整返回地址，最后由返回汇编按照相同布局恢复。

=== 3.3.1 RISC-V64 的 TrapContext
<331-risc-v64-的-trapcontext>
RISC-V64 的上下文需要保存通用寄存器、浮点寄存器、浮点控制状态以及用户态返回所需的 `sstatus` 和 `sepc`：

```rust
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct TrapContext {
    pub x: [usize; 32],
    pub sstatus: Sstatus,
    pub sepc: usize,
    pub f: [usize; 32],
    pub fcsr: usize,
    _padding: usize,
}
```

代码片段 3-1 RISC-V64 的 TrapContext 结构

其中，`x[10]` 是 RISC-V ABI 的 `a0`，既用于传递第一个参数，也用于接收系统调用返回值；`x[2]` 是用户栈指针 `sp`；`sepc` 保存 trap 返回地址，`sstatus` 保存返回时需要恢复的特权级和中断状态。浮点寄存器也属于用户程序的执行现场，因此入口和返回路径需要一并保存、恢复。

创建用户初始上下文时，RespOS 将 `sstatus.SPP` 设置为 User，并关闭保存现场期间的 `SIE`；同时设置入口地址和用户栈指针。对于 Linux ABI 程序，`a0` 等参数由用户栈上的启动数据解释；内置教学程序则可以使用寄存器传递 `argc`、`argv` 等参数。

=== 3.3.2 LoongArch64 的 TrapContext
<332-loongarch64-的-trapcontext>
LoongArch64 的特权状态和返回地址分别由 `prmd` 与 `era` 表示，当前上下文结构保存通用寄存器以及这两个架构状态寄存器：

```rust
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct TrapContext {
    pub x: [usize; 32],
    pub prmd: usize,
    pub era: usize,
}
```

代码片段 3-2 LoongArch64 的 TrapContext 结构

LoongArch 的 `a0` 对应 `x[4]`，栈指针对应 `x[3]`，系统调用号位于 `x[11]`，六个参数位于 `x[4..10]`。`prmd` 中的 PPLV 和 PIE 位用于记录异常前的特权级及返回后的中断状态。

创建 LoongArch 用户上下文时，RespOS 将 `prmd` 初始化为用户态并允许异常返回后重新开启中断，再设置 `era` 和用户栈指针。与 RISC-V 一样，Linux ABI 程序的启动参数从用户栈获取；内置程序则由寄存器传入启动参数。

=== 3.3.3 两种上下文的共同语义
<333-两种上下文的共同语义>
两种结构在字段层面并不完全相同，但它们都提供了访问返回值、用户栈和返回 PC 的语义接口。

图 3-2 总结了 `TrapContext` 在一次 trap 中的生命周期。

```text
硬件写入返回 PC/原因
        │
        ▼
入口汇编保存 TrapContext 到内核栈
        │
        ▼
Rust 读取原因并修改 TrapContext
        │       ├─ 返回值写入 a0
        │       ├─ sepc/era 跳过 ecall
        │       └─ 信号/调度改变待恢复上下文
        ▼
返回汇编按固定布局恢复
        │
        ▼
       sret / ertn
```

图 3-2 TrapContext 在一次 trap 中的生命周期

== 3.4 中断屏蔽与并发安全
<34-中断屏蔽与并发安全>
trap 处理不仅要识别事件，还要避免处理过程被同类事件破坏。RespOS 为两种架构提供统一语义的 `InterruptGuard`：构造时保存当前中断使能状态并关闭中断，离开作用域时仅在原先开启的情况下恢复。这样既能保护临界区，也不会无条件打开调用者原本关闭的中断。

#figure(
  align(center)[#table(
    columns: 4,
    align: (auto,auto,auto,auto,),
    table.header([场景], [RISC-V 机制], [LoongArch 机制], [共同语义],),
    table.hline(),
    [trap 入口], [配置 `stvec`，进入内核入口], [配置 `EENTRY`，进入内核入口], [先建立内核可用的保存环境],
    [中断总开关], [`sstatus.SIE`], [`CRMD.IE`], [由 guard 保存并按原状态恢复],
    [用户态返回], [`sret` 根据 `sstatus` 返回], [`ertn` 根据 `PRMD/ERA` 返回], [只在现场完整后恢复用户态],
    [定时器状态], [SBI 设置下一次触发], [清除定时器状态并配置下一次触发], [重新武装后再执行计时器服务],
  )]
  , kind: table
  )

表 3-2 双架构 trap 控制语义对照

RISC-V 在返回路径强调顺序性约束：恢复寄存器的中间阶段优先保持 `SIE` 关闭，待通用寄存器和浮点寄存器全部恢复后，才切换回用户态。否则定时器可能在现场尚未恢复完成时再次进入 trap，导致使用半恢复状态的寄存器。LoongArch 则在 `ertn` 前准备好 `PRMD`、`ERA`、用户态入口和栈切换信息，再由硬件完成特权级返回。

设备外部中断的具体控制由架构和驱动层共同承担：架构层负责把硬件中断送入统一 trap 路径，驱动层负责识别设备、清除设备状态并完成请求。这样可以把"如何进入内核"和"哪个设备需要服务"分开，避免 trap 分发器依赖具体设备实现。

== 3.5 功能与设计总结
<35-功能与设计总结>
#figure(
  align(center)[#table(
    columns: 3,
    align: (auto,auto,auto,),
    table.header([设计点], [实现方式], [对系统的作用],),
    table.hline(),
    [统一入口], [架构汇编保存现场，Rust 统一分发], [系统调用、异常和中断共享清晰的边界],
    [可修改现场], [`TrapContext` 作为返回上下文], [返回值、信号、抢占都可通过同一机制生效],
    [用户/内核分流], [用户错误转信号或退出，内核错误 panic], [隔离应用故障，尽早暴露内核不变量破坏],
    [架构复用], [共享处理策略，保留各自 CSR 与返回指令], [支持 RISC-V 与 LoongArch 的一致功能语义],
    [返回安全], [固定保存布局、屏蔽顺序和栈切换协议], [避免重入、现场损坏和错误特权级返回],
  )]
  , kind: table
  )

中断与异常模块承担了一条完整的控制流：将硬件事件转换为内核服务，把服务结果写回可恢复现场，最后将控制权安全地交还给任务。内存、时钟、进程、信号和驱动模块则分别在这条闭环的分发节点上实现各自功能。
