[build]
target = "riscv64gc-unknown-none-elf"

[target.riscv64gc-unknown-none-elf]
rustflags = [
    "-Clink-arg=-Tsrc/linker_riscv.ld", "-Cforce-frame-pointers=yes"
]

[profile.dev]
panic = "abort"

[profile.release]
debug = false
lto = "thin"
panic = "abort"

[profile.release-debug]
inherits = "release"
debug = true
