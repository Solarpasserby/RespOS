### 04-07
lorenash: 照着 Rocket 写了一部分，发现全错了。准备照着另一个写，但是我发现需要重构一下代码，包括中断和内存，后者主要是映射内核到高地址
清明假期太摆烂了，希望之后能多花些时间吧，这周把 ext4 搞定是最好的

### 04-08
lorenash:
重构了内存映射部分，主要是照着 RocketOS 写的，这里把内核空间移动到高地址处。我保留了 VirtAddress 的实现，但修改了它和 usize 的转换 
继续阅读了一部分 Rocket 的代码，发现了一些可以用来优化当前内核的部分：
- 我注意到 page_table 中有将内核部分的页表项写入用户空间中的方法，这使得中断发生时可以直接访问内核而无需借助跳板，内核栈似乎也可以统一，之后可以借鉴学习一下
- 这边添加了新的页表项的标志位 COW，我目前还用不到

> 我注意到我借鉴的 Rocket 版本不是最新的，我回溯到了他们实现 pre-2024 的那部分，应该没有太大问题...

### 04-09
lorenash:
今天说实话效率不高，还需再加把劲
这边发现 RocketOS 实现的无中断自旋锁 SpinNoIrq 值得借鉴，我现在使用 spin::Mutex 代替
今天主要补完昨天的工作，把内核迁移到高地址，同时修改了 task 相关内容
和老师聊了一下，目前工作方向应该没有问题，之后会重构中断，实现 ext4 文件系统

### 04-10
lorenash:
在修改中断处理，发现对于异常上下文的存放也需要修改，现在不在用户空间中映射异常上下文段，而是把异常上下文存入内核栈，因此涉及到相当多的 Task 模块的修改
注意到 RocketOS 的 TaskContext 使用了 tp 来记录内核栈上 Task 结构体的位置（我目前是这么理解的），没有使用 sp 来记录任务的内核栈。我目前保留 sp 的实现，之后可以考虑修改
此外，RocketOS 的 KStack 与 pid 不再相关，而是直接存放内核栈指针，但是现在我保留原来的 KernelStack 的实现

现在 TrapContext 不再映射到高地址，而是存放到内核栈管理
现在 TaskContext 仍然保存在 TaskControlBlockInner 中，而非内核栈中。重构了相关的代码，应该可以实现任务切换

### 04-11
lorenash:
基本没怎么改代码（摆烂的一天），中断虽然区分了内核和用户，但是内核异常处理直接 panic

### 04-12
lorenash:
依旧难以集中精力。稍微写了一些完成了对虚拟硬件接口的描述，即 driver 模块，ext4 文件系统的进度大概推到 20%。我应当下定决心，努力去做了

### 04-13
lorenash:
学习实现文件系统时文件的组织方式，了解每个部分的功能，粗略的搭了个架构

### 04-14
lorenash:
说实话看不太懂这个文件系统，只能慢慢啃了

### 04-16
lorenash:
实现文件描述符

### 04-18
lorenash:
完成 sys_read/write
学习 xv6 的文件系统，需要对文件系统建立一个宏观的概念，对文件系统的分层结构有清晰认识

### 04-19
lorenash:
合并分支到 main，布置项目推进任务

### 04-20
lorenash:
创建新分支 feat/fs-open，目标完善文件系统的系统调用，尝试挂载运行比赛环境，~~时限 4 天~~

### 04-24
lorenash:
实现部分系统调用

### 04-28
lorenash:
中途被 archlinux 夺舍了，现在回来继续搞。在新环境开发的过程中发现了原来忽视的 bug，也算是有些许进展吧

### 04-29
lorenash:
今天完成了比赛测试用例镜像的挂载，希望可以把文件系统收口

### 05-06
lorenash:
也是摆了一整个五一，我越来越意识到写一个优秀内核是如此的遥不可及
主要是个人时间的协调，同时需要引导队友学习、花更多的时间攻克
之后大概把文件系统的 bug 修复了，然后移植到不同的架构上

### 05-10
lorenash:
正式回归

### 05-12
lorenash:
我已经被这个 exec 传参的指针干碎了～

### 05-18
lorenash:
最快明天应该可以吧 basic 做完，之后暂时不去做测试用例的内容
我觉得需要补一下同步语义；修改一下线程模型；让队友尝试一下 loongarch 架构的迁移
感觉队友还不是很上道，我不知道做些什么了
