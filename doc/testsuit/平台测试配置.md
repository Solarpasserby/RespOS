先补根 Makefile：make all 生成 kernel-rv，同时处理 .cargo 配置恢复和离线依赖。
改 RISC-V 启动产物为 ELF，并用官方 QEMU 命令本地验证。
把根文件系统设备从 MMIO[1] 改成 MMIO[0]。
写一个非交互 init/testrunner：扫描 /，过滤 *_testcode.sh，按顺序串行执行，打印规定 start/end 标记，最后关机。
vendor 所有 Rust crates/git 依赖，确保无网络也能构建。
最后再推进 kernel-la，这块工作量最大，应单独拆任务。